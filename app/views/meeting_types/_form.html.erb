<%= form_for(@meeting_type) do |f| %>
  <% if @meeting_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meeting_type.errors.count, "error") %> prohibited this meeting_type from being saved:</h2>

      <ul>
      <% @meeting_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :icon %><br>
    <%= f.text_field :icon %>
  </div>
  <div class="field">
    <%= f.label :default_color %><br>
    <%= f.text_field :default_color %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
